<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xia.wenqu.mapper.DocumentMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `document` (kb_id, user_id, name, type, size, status, file_path)
        VALUES (#{kbId}, #{userId}, #{name}, #{type}, #{size}, #{status}, #{filePath})
    </insert>

    <select id="query" resultType="com.xia.wenqu.model.vo.DocumentVO">
        SELECT d.id, d.kb_id, d.name, d.type, d.size, d.status, d.error_msg,
               (SELECT COUNT(*) FROM doc_chunk c WHERE c.document_id = d.id) AS chunk_count,
               CAST(UNIX_TIMESTAMP(d.created_at) * 1000 AS UNSIGNED) AS created_at
        FROM `document` d
        WHERE d.kb_id = #{kbId} AND d.deleted = 0
        ORDER BY d.created_at DESC
    </select>

    <select id="selectById" resultType="com.xia.wenqu.model.vo.DocumentVO">
        SELECT d.id, d.kb_id, d.name, d.type, d.size, d.status, d.error_msg,
               (SELECT COUNT(*) FROM doc_chunk c WHERE c.document_id = d.id) AS chunk_count,
               CAST(UNIX_TIMESTAMP(d.created_at) * 1000 AS UNSIGNED) AS created_at
        FROM `document` d
        WHERE d.id = #{id} AND d.deleted = 0
    </select>

    <select id="selectEntityById" resultType="com.xia.wenqu.model.entity.Document">
        SELECT * FROM `document` WHERE id = #{id} AND deleted = 0
    </select>

    <update id="updateStatus">
        UPDATE `document`
        SET status = #{status}, error_msg = #{errorMsg}
        WHERE id = #{id}
    </update>

    <update id="updateChunkCount">
        UPDATE `document` SET chunk_count = #{chunkCount} WHERE id = #{id}
    </update>

    <update id="updateFilePath">
        UPDATE `document` SET file_path = #{filePath}, status = #{status} WHERE id = #{id}
    </update>

    <select id="selectDeletedByUserId" resultType="com.xia.wenqu.model.vo.RecycleDocVO">
        SELECT d.id, d.kb_id, kb.name AS kb_name, d.name, d.type, d.size,
               (SELECT COUNT(*) FROM doc_chunk c WHERE c.document_id = d.id) AS chunk_count,
               CAST(UNIX_TIMESTAMP(d.deleted_at) * 1000 AS UNSIGNED) AS deleted_at
        FROM `document` d
        LEFT JOIN knowledge_base kb ON kb.id = d.kb_id
        WHERE d.user_id = #{userId} AND d.deleted = 1
        ORDER BY d.deleted_at DESC
    </select>

    <select id="selectDeletedEntityById" resultType="com.xia.wenqu.model.entity.Document">
        SELECT * FROM `document` WHERE id = #{id} AND deleted = 1
    </select>

    <update id="softDelete">
        UPDATE `document` SET deleted = 1, deleted_at = NOW() WHERE id = #{id} AND deleted = 0
    </update>

    <update id="restoreById">
        UPDATE `document` SET deleted = 0, deleted_at = NULL WHERE id = #{id} AND deleted = 1
    </update>

    <delete id="deletePhysically">
        DELETE FROM `document` WHERE id = #{id}
    </delete>

    <select id="selectDeletedByKbId" resultType="com.xia.wenqu.model.entity.Document">
        SELECT * FROM `document` WHERE kb_id = #{kbId} AND deleted = 1
    </select>

    <update id="restoreByKbId">
        UPDATE `document` SET deleted = 0, deleted_at = NULL WHERE kb_id = #{kbId} AND deleted = 1
    </update>

    <delete id="deletePhysicallyByKbId">
        DELETE FROM `document` WHERE kb_id = #{kbId}
    </delete>
</mapper>