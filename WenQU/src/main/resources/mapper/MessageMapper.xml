<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xia.wenqu.mapper.MessageMapper">
    <select id="selectByConversationId" resultType="com.xia.wenqu.model.entity.Message">
        SELECT id, conversation_id, role, content, sources, created_at
        FROM message
        WHERE conversation_id = #{conversationId}
        ORDER BY id ASC
    </select>

    <select id="selectRecent" resultType="com.xia.wenqu.model.entity.Message">
        SELECT id, conversation_id, role, content, sources, created_at
        FROM message
        WHERE conversation_id = #{conversationId}
        ORDER BY id DESC
        LIMIT #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO message (conversation_id, role, content, sources)
        VALUES (#{conversationId}, #{role}, #{content}, #{sources})
    </insert>

    <delete id="deleteByConversationId">
        DELETE FROM message WHERE conversation_id = #{conversationId}
    </delete>
</mapper>
