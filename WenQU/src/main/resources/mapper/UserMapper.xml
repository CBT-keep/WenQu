<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xia.wenqu.mapper.UserMapper">

    <select id="findByUsername" resultType="com.xia.wenqu.model.entity.User">
        SELECT id, username, password, nickname, role, status, created_at, updated_at, deleted
        FROM `user`
        WHERE username = #{username} AND deleted = 0
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO `user` (username, password, nickname, role, status)
        VALUES (#{username}, #{password}, #{nickname}, #{role}, #{status})
    </insert>

    <select id="countByUsername" resultType="int">
        SELECT COUNT(*) FROM `user`
        WHERE username = #{username} AND deleted = 0
    </select>

</mapper>